# Claude Roster Agent Report

## Not generated yet

Run the **Claude Roster Agent** workflow from the GitHub Actions tab, or wait for it to run after the next successful **NCS Roster Watch** workflow.

## Setup required

Add `ANTHROPIC_API_KEY` as a repository secret to enable live Claude analysis.

The agent will still create a fallback team-level report if the secret is missing.
